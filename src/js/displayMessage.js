// import findMessage from "./getMessages"

// export default function displayMessage(todayData) {
//   //fetch icon
//   const weather = todayData.weather[0].icon;
//   const message = findMessage(weather);

  
//   const message_top = document.querySelector('.message_top');
//   const message_weatherType = document.querySelector('.message_weatherType');
//   const message_bottom = document.querySelector('.message_bottom')

//   // message_top.textContent = message[0];
//   // message_weatherType.textContent = message[1];
//   // message_bottom.textContent = message[2];
// } 